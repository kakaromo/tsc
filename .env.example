# Azure Speech 발음평가용 키 — 아래 두 값을 실제 값으로 채워 .env 파일에 저장하세요.
# (이 .env.example은 안내용이며 git에 올라갑니다. 실제 키는 .env에만 넣으세요.)
AZURE_SPEECH_KEY=여기에_KEY_1_값
AZURE_SPEECH_REGION=koreacentral

# 앱 접속 비밀번호. 설정하면 로그인 화면이 뜨고, 없으면 보호 없이 열림.
APP_PASSWORD=원하는_비밀번호
